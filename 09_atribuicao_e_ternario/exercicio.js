// Some 500 ao valor de scroll abaixo atribuindo o novo valor a scroll
var scroll = 1000;
scroll += 500;
console.log(scroll);

// Atribua true para a variável darCredito caso o cliente possua carro e casa.
var casa = true;
var carro = false;
var darCredito =  (casa === true && carro === true) ? 'Crédito liberado' : 'Crédito não liberado';
console.log(darCredito);