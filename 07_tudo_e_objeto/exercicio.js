// Nomeie 3 propriedades ou métodos de strings
var nome = 'Adriel';
// fixed
// length
// slice 

// Nomeie 5 propriedades ou m étodo0s de elementos do DOM
var btn = document.querySelector('.btn');
//addEventListener
// appendChild
// id
// innerHTML
// auterHTML


// Busque na web um objeto (método) capaz de interagir com o clipboard que é a parte do seu computador que lida com o CTRL + C e CTRL + V

addEventListener('clique', function() {
    var copia = new ClipboardJS('.btn');
})
